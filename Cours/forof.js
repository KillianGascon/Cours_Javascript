// Définir un Set contenant des fruits
const fruitsSet = new Set(["Pomme", "Banane", "Poire", "Fraise", "Kiwi", "Ananas", "Mangue", "Framboise", "Cerise", "Raisin"]);

// Utiliser la boucle for...of pour afficher chaque fruit
for (let fruit of fruitsSet) {
    console.log(fruit);
}