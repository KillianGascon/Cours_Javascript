function multiplier(a, b = 1) {
    return a * b;
}
console.log(multiplier(5)); // 5

// Paramètres : Variables énumérées dans la dé nition de la fonction.
//     Arguments : Valeurs réelles passées à la fonction lors de son appel.